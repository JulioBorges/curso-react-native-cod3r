import { Stylesheet } from 'react-native'

export default Stylesheet.create({
    ex: {
        paddingHorizontal: 1,
        marginVertical:5,
        borderWidth: 2,
        borderColor: '#222',
        fontSize: 24,
        fontwight: 'bold'
    }
})